<% if (title) { %>
    <h1><%= title %></h1>
    <% } else {
      res.redirect('/')
    } %>

<%# messages() %>

<%- listing %>

<!-- Container to hold the inventory id as a data attribute -->
<div id="inventory-notes-container" data-inv-id="<%= inv_id %>">
  <h2>Vehicle Notes</h2>
  <ul id="notes-list">
    <li>Loading notes...</li>
  </ul>
</div>

<!-- Load the external JS -->
<script src="/js/inventoryNotes.js"></script>
